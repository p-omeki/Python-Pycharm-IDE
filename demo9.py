#Passing arguments in python
def add(a, b):
    print(a+b)

add(10,20) #Here, we specify the argument values for a and b pass them as arguments.

#You can also pass several arguments in that one single defined function...
add(100, 200) #This allows you pass several arguments using the same function